# Claude Code project instructions

@AGENTS.md

## Claude-specific behavior

- Start substantial changes in Plan mode.
- Use the `context-researcher` agent for broad codebase exploration.
- Use the `code-reviewer` agent after implementation.
- Use `/implement-coding-task` for approved briefs.
- Keep progress visible as a short task list.
- Stop at the approval checkpoints defined in the brief.
