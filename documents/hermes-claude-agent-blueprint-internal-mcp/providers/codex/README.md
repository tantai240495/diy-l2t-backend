# Future Codex Adapter

Implement the same contract as `providers/claude-code/`.

Do not change:

- coding brief schema;
- job state;
- workflows;
- project registry;
- approval checkpoints;
- security rules.

Only translate:

- launch command;
- provider-specific project instruction file;
- session/resume behavior;
- tool permission syntax;
- streamed event format.
